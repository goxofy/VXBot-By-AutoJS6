{
  "name": "vchat",
  "version": "1.0.0",
  "description": "vchat is a chat ui automation testing framework.",
  "main": "index.js",
  "scripts": {
    "test": "esbuild test/test.js --bundle --platform=node --target=node18 --format=cjs --outfile=dist/main.js",
    "demo": "esbuild demo.js --bundle --platform=neutral --target=es2015 --format=cjs --outfile=dist/VXBot.js",
    "flow2api:gemini": "node test/flow2api_gemini.js"
  },
  "keywords": [
    "vchat",
    "hamibot",
    "autojs",
    "robot",
    "wxrpa"
  ],
  "author": "tmkook",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.12"
  }
}
